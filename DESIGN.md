# webmux 选型与取舍

这份文档只回答「为什么这么选」。怎么用看 [README](README.md)，怎么装看 [安装与配置](docs/install.md)，
实现时踩过的坑看 [踩过的坑](docs/gotchas.md)。

## 为什么底座是 tmux

进程常驻、scrollback、多客户端 attach、SSH 也能连同一个会话——这四样全是白送的。
自己用 node-pty 直接托管进程也能做出终端，但「关掉网页进程还在」「从 SSH 连回同一个
会话」都得自己重造，而 tmux 已经把这件事做了二十年。

代价是接受 tmux 的固有边界：tmux server 被 kill 或机器重启，终端进程就没了。
真要跨重启的东西应该交给 nohup/systemd，不该指望一个终端复用器。

## 为什么不用 ttyd + tmux

ttyd 给的是「一个网页一个终端」，拿不到侧栏、标签、活动角标、前端布局分屏——
而那些恰恰是主体。webmux 要的不是「网页上有个终端」，是「网页上有一套会话管理」。

## 为什么分屏用前端布局，不用 tmux 的 split-window

一个格子 = 一个独立 tmux session，前端并排放两个 xterm。

这样分割线是 DOM（可拖拽），每个格子有各自独立的尺寸。用 tmux 自己的 split，
整块画布只有一个尺寸，缩放会很别扭；而且跨工作区移动一个 pane 在 tmux 里是
`join-pane` 那套，比在前端搬一个 id 麻烦得多。

## 为什么布局树是二叉的

拖分割线时只需调整一个 ratio，不用在 N 个兄弟之间做等比重分配。嵌套两层就能
表达任意 IDE 式布局，多出来的表达力买不到什么。

## 为什么只有布局落盘，会话不落盘

tmux 是会话的唯一真相。存了 id 只会在会话被 kill 后留下指向空气的标签——而
「从 SSH 手工 kill 掉一个会话」是个真实会发生的事。每次读状态都拿 `tmux ls`
对账（reconcile），代价是每次多 fork 一次 tmux，换来的是永远不会有幽灵标签。

状态文件因此是可再生的，版本对不上就直接重置，不为它写迁移。

## 为什么活动广播是轮询，不是 tmux 的 monitor-activity

`monitor-activity` 要写进用户的 tmux 配置，语义也只到「非当前窗口有动静」。
一次 `list-sessions` 就够画出「有新输出 / 已退出 / 正在跑什么」三种角标，
还不污染任何 tmux 配置。1 秒一次、内容没变就不推，终端空闲时这条广播完全静默。

## 为什么后端不推「未读」

哪个标签正被人看着，只有前端知道。后端也维护一份的话，两边迟早对不上——
尤其是同一个会话被两个浏览器同时开着的时候。后端只推事实（activityAt），
未读由前端拿 `lastSeen` 算。

## 为什么二进制帧=键盘、文本帧=控制消息

不靠「能不能解析成 JSON」区分。在 shell 里敲一段 JSON 是很正常的事，用内容
猜的话那行输入会被当成控制消息吃掉，而且这种 bug 极难复现。

## 为什么标题默认跟随而不是「终端 1、终端 2」

一屏四个格子的时候，「终端 3」不携带任何信息。跟着终端里在跑什么走，才让人
一眼知道哪个是编译、哪个是 vim、哪个是空闲的 shell。

代价是标题会动，而且要处理程序自报标题里的 spinner 动画字符——那部分逻辑
（`autoLabel` / `stripDecor` / `isShellChrome`）是全项目单测最密的地方。

## 为什么两套键位

Mac 上终端只用 Ctrl，`⌘` 那一整层是空的，随便拿。其它平台没有这么一层：
裸 `Ctrl+字母` 在终端里全都有含义（`Ctrl+C` 中断、`Ctrl+D` EOF、`Ctrl+W` 删词、
`Ctrl+K` 删到行尾），抢任何一个都等于把终端弄残，所以只能用 `Ctrl+Shift`。

而 `Ctrl+Shift` 里 Shift 已经是修饰键的一部分，不能再拿它区分条目——所以 Mac 上
靠 `⇧`/`⌥` 区分的几条，在非 Mac 上必须换成不同的键。

## 为什么自己画输入框而不用 prompt/alert

原生对话框会阻塞页面，更要命的是全屏时弹出会让浏览器退出全屏——按一次重命名
就掉出键盘独占，`⌘T` 跟着失效。

## 为什么图片上传是 base64 JSON 而不是 multipart

Node 标准库没有 multipart 解析器，为一张截图手写一个不划算。base64 膨胀 33%，
对几百 KB 的截图无所谓。

## 为什么令牌不拦静态资源

静态是不含数据的前端 bundle；而且 iframe 嵌入时 `SameSite=Lax` 的 cookie 不会
随子资源请求发送，拦静态会让嵌入页白屏——页面能开、`/assets/*` 全 401，
这种形态排查起来非常费劲。

## 明确不做

多用户与权限体系（单用户自用，token 即门槛）、会话录制回放与审计日志、
移动端专门适配、跨机器重启的持久化。
